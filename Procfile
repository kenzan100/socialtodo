web:          bundle exec rails server thin -p $PORT
# worker:       bundle exec rake environment resque:work QUEUE=*
# urgentworker: bundle exec rake environment resque:work QUEUE=urgent
# clock:        bundle exec clockwork clock.rb
