<h3>自分のタスク一欄</h3>

<table>

<% @tasks.each do |task| %>
  <tr>
    <td><img src="https://graph.facebook.com/<%= task.user.facebook_id %>/picture" width="20" height="20"></td>
    <td><%= task.description %></td>
    <td><%= link_to '削除', task, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>
